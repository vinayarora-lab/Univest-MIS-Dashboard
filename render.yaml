services:
  - type: web
    name: Univest-MIS-Dashboard
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
